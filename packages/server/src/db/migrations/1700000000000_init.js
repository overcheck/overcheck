// Scaffold migration: proves the migration pipeline runs at boot.
// The check-engine milestone adds the first real tables (monitors, checks, alerts) here.

export const up = (_pgm) => {}

export const down = (_pgm) => {}
